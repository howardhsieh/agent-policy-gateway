"""Worked examples for agent-policy-gateway.

This package is *not* shipped as part of the wheel — it lives next to the
source so the runnable demonstrations can import the public API the same
way users would. Each subpackage is a self-contained scenario.
"""
