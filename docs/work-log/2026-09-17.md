# 2026-09-17 — R58: README / portfolio upgrade

**Item.** R58 (first unchecked item under "Up next"): rewrite the README
as the project's front page for recruiters and researchers. Docs-only —
no behavioral code changes, test count unchanged (baseline today:
1493 passed, 2 skipped, ruff + mypy + `mkdocs build --strict` green).

## Plan

- **Pitch**: one opening paragraph that names the headline research
  result — the R56/R57 seven-arm comparison and its Pareto frontier,
  with the `apg-value-taint` arm at 100% utility / 0% compromise on the
  measured family.
- **Badges**: license (Apache-2.0), Python (3.10+), Semgrep CI (the only
  push-triggered workflow), all shields/actions-badge based.
- **Architecture**: Mermaid flowchart of the reference monitor — policy
  engine, dual-label session + per-value taint, call history, audit log,
  protocol adapters — replacing the ASCII sketch (which stays in
  `docs/design.md`).
- **60-second demo**: copy-paste block (`git clone` → `pip install -e .`
  → `python -m examples.indirect_injection`) with the exact two-line
  expected output, verified in this run; plus the one-command
  reproduction of the headline table
  (`python -m agent_policy_gateway.comparison_benchmark`).
- **Results table**: the R56+R57 utility/compromise summary for all
  seven arms, numbers copied verbatim from
  `docs/benchmarks/comparison.md`, linked prominently along with
  `docs/threat-model.md` and `ROADMAP.md`.
- **Verify**: README renders on GitHub (Mermaid + tables are standard
  GFM); `python -m pytest -q`, `ruff`, `mypy`, `mkdocs build --strict`
  all stay green; CHANGELOG + ROADMAP move; push `HEAD:main`.

## Result — R58 complete

Implemented as planned; all acceptance criteria met, docs-only.

- **README rewritten** as the front page: pitch paragraph naming the
  R56/R57 headline (measured Pareto frontier; `apg-value-taint` at
  100% utility / 0% compromise), seven-arm utility/compromise summary
  table (numbers verbatim from the pinned
  `docs/benchmarks/comparison.md` matrices), Mermaid architecture
  diagram replacing the ASCII sketch, 60-second demo with the exact
  expected two-line output (re-verified in this run:
  `python -m examples.indirect_injection` after a bare
  `pip install -e .`), one-command headline reproduction
  (`python -m agent_policy_gateway.comparison_benchmark`, ~1.6 s),
  badges (license / Python 3.10+ / Semgrep workflow), and prominent
  links to `docs/threat-model.md`, `docs/benchmarks/`, `ROADMAP.md`.
  The quick-start snippet was also corrected to the real API
  (`Gateway(policies=[load_policy(...)])` + `ToolTaintSpec.of`) — the
  old README showed a `Gateway.from_yaml`/`taint_sources` API that
  doesn't exist.
- **Structure kept for pinned tests**: `## Release` section +
  `docs/release.md` link (`test_release_runbook.py`) and the
  threat-model reference (`test_threat_model.py`) preserved.
- **Verification**: pytest exit 0 (1495 collected: 1493 passed,
  2 skipped — unchanged), ruff clean, mypy clean (24 files),
  `mkdocs build --strict` clean, all README relative links resolve
  (scripted check).
- **Observation (out of scope today)**: `agent_policy_gateway.__version__`
  is `"0.0.1"` while `pyproject.toml` says `0.1.0` — a one-line
  discrepancy worth folding into the next code-touching day.

## 明日接續 (next run)

R59 — model-in-the-loop long-horizon eval (first unchecked item in
"Up next"): thin LLM driver interface + recorded/replay fixtures (CI
deterministic, key-free), utility/ASR for no-defense vs one APG arm on
a small AgentDojo-composed slice via `scenario_from_suite`, token +
wall-clock cost per arm, behavior write-up in `docs/benchmarks/`.
