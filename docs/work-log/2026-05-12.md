# 2026-05-12 — R14 blocked (3rd consecutive day): PAT lacks `workflow` scope

## Roadmap item
**R14. PyPI publish workflow.**

> See also: [`2026-05-10.md`](2026-05-10.md) and
> [`2026-05-11.md`](2026-05-11.md). Today's run hit the **same
> credentials blocker** as the previous two attempts. R14 is the
> first unchecked item under "Up next" and the daily task keeps
> picking it up; the only thing preventing the push is the GitHub
> PAT scope.

## What today's run did

Implemented R14 from scratch again in `/tmp/apg-run-2026-05-12` and
verified locally before attempting the push:

- New `.github/workflows/publish.yml`: tag-driven (`v*`) and
  `workflow_dispatch` triggers; three sequential jobs
  (`test → build → publish`); `test` installs the `[dev]` extra and
  runs `pytest -q` + `ruff check`; `build` runs `python -m build` and
  uploads `dist/` as a workflow artifact; `publish` downloads the
  artifact and uses
  `pypa/gh-action-pypi-publish@release/v1` with
  `permissions: id-token: write` for PyPI trusted publishing; the
  publish job is pinned to a `pypi` GitHub Environment so a
  required-reviewer protection rule can gate the upload.
- New `docs/release.md`: full release runbook (one-time PyPI
  trusted-publisher configuration, the steady-state tag-and-push flow,
  the manual `python -m build` + `twine upload` fallback, and the
  post-release verification checklist). Wired into the mkdocs `nav`
  after Daily runbook; linked from a new *Release* section in
  `README.md` and a new *Cut a release* row in `docs/index.md`'s
  where-to-start table.
- `docs/design.md` gains a "Publish workflow (R14)" note recording
  the trusted-publishing-over-stored-tokens decision and the
  `test → build → publish` chain.
- 25 new tests in `tests/test_publish_workflow.py` lock in structural
  invariants of the workflow file and the release docs.

Local verification (working tree, before the push attempt):

- `python3 -m pytest --basetemp=/tmp/apg-pytest` → **394 passed,
  1 skipped** (369 → 394, +25 new R14 tests).
- `python3 -m ruff check .` → **All checks passed**.
- `python3 -m build` → produces `agent_policy_gateway-0.0.1.tar.gz`
  and the matching wheel cleanly. This is the same code path the
  workflow's `build` job would run.

## The blocker (same as 2026-05-10 and 2026-05-11)

```
! [remote rejected] main -> main
  (refusing to allow a Personal Access Token to create or update
   workflow `.github/workflows/publish.yml` without `workflow` scope)
```

GitHub refuses, atomically, any push that creates or updates a file
under `.github/workflows/` if the PAT does not carry the `workflow`
scope. The PAT in `.gh-token` does not have it (the original
`SETUP.md` only asks for `Contents: Read and write` and
`Pull requests: Read and write`). Nothing else in the run failed.

## Decision

- The R14 commit was rolled back with `git reset --soft HEAD~1`
  followed by `git reset HEAD` and the speculative `ROADMAP.md` /
  `CHANGELOG.md` edits were reverted, so `main` does not falsely
  claim R14 shipped.
- The actual deliverable files
  (`.github/workflows/publish.yml`, `docs/release.md`,
  `tests/test_publish_workflow.py`, `mkdocs.yml`, `README.md`,
  `docs/index.md`, `docs/design.md`) are mirrored back to the
  Github folder so they are visible locally and ready for a
  human push after the PAT is updated.
- This work-log is the *only* file in today's commit, because a
  docs-only push does not touch `.github/workflows/` and therefore
  does not trip the scope check — so today's blocker is at least
  visibly recorded on `origin/main`, even when the R14 work itself
  cannot land.

## Action required by Howard (third reminder)

The same one-line change as 2026-05-10 and 2026-05-11 will unblock
the next scheduled run:

- **Classic PAT** (`ghp_…`): tick the **`workflow`** scope (sits
  under `repo`).
- **Fine-grained PAT**: set **Workflows: Read and write** under
  *Repository permissions* in addition to the existing
  *Contents: Read and write*.

Then replace the `GITHUB_TOKEN=...` line in `.gh-token` with the
new value. Nothing else in the project needs to change. The next
scheduled run will pick R14 from "Up next" and ship the same set
of files in one push.

## Final state

- Remote `main`: receives only this work-log on 2026-05-12.
- ROADMAP / CHANGELOG: unchanged on `main` — R14 is still under
  *Up next*.
- Tests on `main` after this docs commit: unchanged at 369 passed,
  1 skipped.
- Local Github folder: contains the prepared R14 files (workflow,
  release docs, tests) ready for manual push by Howard, or for the
  next scheduled run to re-attempt once the PAT scope is fixed.
