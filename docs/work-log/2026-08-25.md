# 2026-08-25 — Plan revision (2026-08-21) + R49a: AgentDojo runtime adapter

**Context.** First run under Howard's 2026-08-21 plan revision: audit-stats /
CLI-polish items and new generic adapters are stopped (R48 parked), and the
project pivots to three phases — Phase 1: R49 AgentDojo adapter → R50
no-defense vs APG attack-success-rate / utility numbers in `docs/benchmarks`;
Phase 2: R51–R54 (dual-label taint, declarative declassify, chain-level
policies, Progent rule import PoC); Phase 3: R55–R56 (long-horizon stateful
adversarial harness, APG/Progent/Fides comparison). R49 is split into daily
sub-items in ROADMAP "Up next"; today is **R49a**, the core runtime wrapper.

## Plan

- Restructure `ROADMAP.md` "Up next": record the revision, park R48, list
  R49a–c / R50 (Phase 1) and R51–R56 as items.
- Fix the 7 pre-existing broken doc links that fail
  `test_mkdocs_build_clean` under mkdocs-material 9.7's strict link checks
  (work-logs linking never-committed `2026-05-10/11.md`; `design.md` linking
  outside `docs/`).
- New `src/agent_policy_gateway/agentdojo_adapter.py`: a
  `GatedAgentDojoRuntime` that duck-types AgentDojo's `FunctionsRuntime`
  surface (`functions`, `register_function`,
  `run_function(env, function, kwargs, raise_on_error=False) ->
  (result, error | None)`) and mediates every `run_function` through
  `Gateway.execute`. AgentDojo is **not** a runtime dependency (verified
  against the real installed API, tested against fakes, like R21).
- Policy refusals surface in AgentDojo's native error convention:
  `("", "PolicyDenied: …")` / `("", "PolicyReview: …")` when
  `raise_on_error=False`, re-raised otherwise — so a blocked injected call
  becomes model-visible feedback and the episode continues (what R50 needs
  to measure utility-under-defense).
- **Session-level taint accumulation**: the model's LLM loop can't thread
  `apg_input_label`, so the wrapper keeps a cumulative `TaintLabel` — each
  executed call's `decision.output_label` becomes the next call's input
  label (denied calls don't taint; declassify drops sources). `taint_label`
  property + `reset_taint()` for per-episode reset.
- Tests (`tests/test_agentdojo_adapter.py`, fakes mirroring the real
  `(result, error)` contract): passthrough, allow/deny/review, error
  formats, taint accumulation incl. the canonical injected-email →
  exfiltration-denied-at-send scenario, resource binding, audit integration.
- `__init__.py` exports, `docs/design.md` note, CHANGELOG, ROADMAP move.

## What shipped

Everything planned. Decisions worth recording (all three now in
`docs/design.md` "AgentDojo adapter (R49a)"):

1. **Refusals return, they don't raise.** The refusal string includes the
   rule id (`"PolicyDenied: refused by rule '<id>': <reason>"`) so R50 can
   attribute refusals straight from episode transcripts; a defense that
   crashed the episode would make utility-under-defense unmeasurable.
2. **Taint accumulates per session.** Deliberately conservative: once
   untrusted content enters the conversation, everything after is treated
   as potentially influenced (threat model A1). Denied *and errored* calls
   leave the label unchanged. Finer tracking is R51/R53 territory.
3. **`_invoke` calls the inner runtime with `raise_on_error=True` and
   forwards tool args as `**kwargs`**, so a REDACT rule masks fields by
   argument name before the runtime sees them, and tool errors are
   re-rendered in the runtime's own `"ErrorType: message"` shape on the
   `raise_on_error=False` path.

Also done, pre-flight: fixed the 7 pre-existing broken doc links that
failed `test_mkdocs_build_clean` under mkdocs-material 9.7 (commit
`2b8211b`) and gitignored the mkdocs-generated `site/` directory.

The real `agentdojo` package (installed locally only to verify the
`run_function` signature — it is not a dependency) confirmed the
`(result, error)` tuple contract and the `"ErrorType: message"` format.

## Files touched

- `src/agent_policy_gateway/agentdojo_adapter.py` (new, ~230 lines)
- `src/agent_policy_gateway/__init__.py` — three new re-exports
- `tests/test_agentdojo_adapter.py` (new, 23 tests)
- `docs/design.md` — "AgentDojo adapter (R49a)"
- `docs/work-log/2026-05-12/13/16.md`, `docs/design.md` — link fixes
- `ROADMAP.md` (revised "Up next" per the 2026-08-21 plan; R48 parked;
  R49a → Done), `CHANGELOG.md`, `.gitignore`

## Verification

`python3 -m pytest` — 961 passed, 2 skipped (938 before the new tests;
+23). `python3 -m ruff check .` — clean. `python3 -m mypy
src/agent_policy_gateway` — clean. `python3 -m mkdocs build --strict` —
clean.

## 明日から接續 (next run)

**R49b — AgentDojo suite wiring** is the first unchecked item: against the
real `agentdojo` package (dev/optional extra only), register a task suite's
tools through `GatedAgentDojoRuntime`, derive per-suite `ToolTaintSpec`s
(readers of injectable content add `agentdojo:untrusted`; external sinks
are the deny surface), ship `policies/agentdojo.yaml` and a runnable
`examples/agentdojo/`, with integration tests skipped when `agentdojo` is
not importable. The adapter surface R49b builds on is exactly
`wrap_agentdojo_runtime` + `reset_taint()` per episode.

## Completion summary

Shipped **R49a — AgentDojo runtime adapter (core wrapper)**, the first item of the 2026-08-21 plan revision: `GatedAgentDojoRuntime` mediates every AgentDojo `run_function` call through the gateway, returns policy refusals in AgentDojo's native error convention with the rule id spelled out, and accumulates a session-level taint label so injected content read early poisons later sink calls without the LLM loop threading labels. ROADMAP restructured (R48 parked; R49a–c/R50 and R51–R56 phases listed); 7 pre-existing broken doc links fixed to restore `mkdocs --strict`. Test count 938 -> 961 (+23); ruff + mypy + mkdocs-strict clean.
