# Work log — 2026-06-17

## Roadmap item
**R36. `apg audit stats --agent <id>` agent filter.**

## Plan
- Add pure `filter_by_agent(records, patterns)` helper in `audit.py`, mirroring
  `filter_by_tool` (R35): fnmatch glob union over `call.agent_id`, falsy
  `patterns` means no filter, order-preserving, materialized to a list.
- Reuse the `_NO_AGENT` sentinel (R33): records whose `agent_id` is falsy are
  matched against the patterns under the `_NO_AGENT` label, so the unattributed
  bucket is selectable with an explicit `--agent '(unattributed - no agent_id)'`.
- Export `filter_by_agent` from `audit.py` `__all__` and the package root.
- Wire a repeatable `--agent GLOB` option into `apg audit stats`, applied after
  the existing `--verdict/--since/--until/--tool` filters; document the sentinel
  in `--help`.
- Tests in `tests/test_audit_stats.py`: pure-helper class (incl. None bucket via
  sentinel) + CLI class (text, --json, repeatable union, composes with --tool,
  no-match empty log).

## Files to touch
- `src/agent_policy_gateway/audit.py`
- `src/agent_policy_gateway/__init__.py`
- `src/agent_policy_gateway/cli.py`
- `tests/test_audit_stats.py`
- `ROADMAP.md`, `CHANGELOG.md`, this work-log.

## Shipped
- `filter_by_agent(records, patterns)` added to `audit.py` and exported from the
  package root; fnmatch glob union over `call.agent_id`, unattributed records
  matched under the `_NO_AGENT` sentinel (R33 reuse).
- `apg audit stats` gained a repeatable `--agent GLOB` option, applied after
  `--verdict/--since/--until/--tool`, with the sentinel documented in `--help`.
- 17 new tests (`TestFilterByAgent`, `TestAuditStatsAgentCli`). Test count
  680 -> 697 (+17). ruff + mypy clean.
