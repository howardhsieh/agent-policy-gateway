# Work log — 2026-06-09

## Context: "Up next" was exhausted — proposing a new milestone batch (R30–R33)

On entry, every roadmap item R1–R29 was already in **Done** and the `## Up next`
section carried only the note left by the prior run: _"All proposed items
complete — see Done. Propose new milestones when starting the next run."_

This is the project's established, twice-documented precedent (the 2026-06-01
R22–R25 batch and the 2026-06-05 R26–R29 batch): when "Up next" empties, the
run **proposes a fresh batch of small milestones and implements the first one
in the same run**, exactly as the R26 proposing run did. I followed that
precedent rather than ending with no push, because the prior run explicitly
instructed it in the ROADMAP and a verified, green, shipped increment is the
higher-value outcome.

New batch proposed (smallest-first, each a self-contained ~1-run increment):

- **R30** — `apg audit stats --json` machine-readable output. *(implemented today)*
- **R31** — `apg audit stats --verdict <v>` filter (summarize only matching records).
- **R32** — `apg audit stats` reads from stdin when `log` is `-`.
- **R33** — `summarize_audit` "busiest agent" line (top agents by hit count).

## Item shipped today: R30 — `apg audit stats --json`

### Plan
- Add a pure helper `audit_stats_dict(records, *, source=None, top_n=5) -> dict`
  in `audit.py` returning the same statistics `summarize_audit` renders, as a
  structured dict (source, records, span, per-verdict count+pct for all four
  verdicts in enum order, deny+review share, top rules, top tools). Logic-only,
  no I/O — mirrors the `summarize_audit` / `cli._explain` / `cli._lint` pattern.
- Export `audit_stats_dict` from `audit.__all__` and the package `__init__`.
- Add a `--json` flag to the `apg audit stats` subparser. When set,
  `_cmd_audit_stats` prints `json.dumps(audit_stats_dict(...), indent=2)`
  instead of the plain-text lines. Exit codes unchanged (0 ok, 2 missing
  file, 3 malformed log).
- Empty log under `--json` emits `{"source": ..., "records": 0}` (no span /
  verdict keys), paralleling the text summary's empty-log shortcut.
- Add tests to `tests/test_audit_stats.py` covering the pure helper and the
  `--json` CLI path.

### Files
- `src/agent_policy_gateway/audit.py` — new `audit_stats_dict`, `__all__` entry.
- `src/agent_policy_gateway/__init__.py` — re-export.
- `src/agent_policy_gateway/cli.py` — `--json` flag + JSON branch, new imports.
- `tests/test_audit_stats.py` — new tests.
- `ROADMAP.md`, `CHANGELOG.md` — batch proposal + R30 move to Done.
