# 2026-09-20 — R61: External write-up packaging

**Item.** R61 (first unchecked item under "Up next"): turn
`docs/benchmarks/comparison.md` + the R50/R53 AgentDojo numbers into a
self-contained workshop-style write-up under `docs/paper/`: related-work
section (Progent, Fides, CaMeL, f-secure/IsolateGPT lineage), one
reproducibility script per figure/table, pinned environment; fold in the
R57 `value-taint` arm, the R59 model-in-the-loop numbers, and the R60
overhead numbers. Acceptance: `docs/paper/` draft builds; every number
in it regenerated by a committed command.

## Plan

- **`docs/paper/index.md`** — the draft itself, workshop-paper shape:
  abstract → introduction → related work (Progent, Fides, CaMeL,
  f-secure/IsolateGPT isolation lineage, AgentDojo) → system overview
  (reference monitor, dual-label taint, declassify, chain history,
  per-value ledger) → evaluation with five numbered tables, each
  carrying its own "Reproduce" command: **T1** AgentDojo no-defense vs
  APG + chain arm (R50/R53), **T2** long-horizon stateful (R55),
  **T3** seven-arm comparison (R56/R57), **T4** model-in-the-loop
  (R59), **T5** overhead ladder + scaling (R60) → discussion,
  limitations (scripted replay, exact-match propagation, simulated
  driver caveat), future work (R62 TraceSig pairing).
- **`docs/paper/repro/`** — one committed script per table
  (`table1-agentdojo.sh` … `table5-overhead.sh`) plus `all.sh` and
  `environment.sh` (records Python/package/seed pins); a
  `requirements.lock` freezing the environment the pinned numbers came
  from.
- **Consistency enforced by tests** (`tests/test_paper.py`): every
  paper table cell must equal the source-of-truth value — computed
  in-process for the deterministic agentdojo-free benchmarks
  (stateful R55, comparison R56/R57), from replay fixtures for R59
  (skipped without `agentdojo`), and textually against the pinned
  benchmark docs for R50 and machine-specific R60; every repro script
  must exist, parse (`bash -n`), and reference real modules.
- **Wiring**: mkdocs nav gains a "Paper" entry; `not_in_nav` untouched;
  CHANGELOG; ROADMAP move; design.md R61 section.
- **Verify**: `python -m pytest -q`, `python -m ruff check .`,
  `python -m mypy`, `mkdocs build --strict` all green; Conventional
  Commit; push `HEAD:main`.

## Result — R61 complete

Implemented as planned; all acceptance criteria met.

- **The draft** (`docs/paper/index.md`, ~490 lines, mkdocs nav
  "Paper"): *Labels, Histories, and Values: Measuring Policy Paradigms
  for Prompt-Injection Defense in Tool-Calling Agents*. Abstract with
  the three organizing findings (launderability predicts long-horizon
  robustness; session state strictly dominates the equivalent
  stateless allowlist; covert exfiltration is a session-scope
  impossibility that per-value labels break); related work — all six
  references (AgentDojo, Progent, Fides, f-secure, CaMeL, IsolateGPT)
  **verified against their arXiv records via web search before
  citing** (author lists, titles, IDs); threat model; system overview
  (R51 dual labels, R52 declassify, R53 chain history, R57 value
  ledger); five numbered tables with inline reproduce commands — T1
  AgentDojo + slack chain arm (R50/R53), T2 laundering horizon (R55),
  T3 seven-arm comparison (R56/R57), T4 model-in-the-loop with the
  simulated-driver provenance warning carried verbatim (R59), T5
  overhead ladder (R60); limitations (scripted replay, exact-match
  propagation boundary, arms-are-renderings); future work (R62
  TraceSig); reproducibility appendix pinning the environment.
- **Repro scripts** (`docs/paper/repro/`): `table1-agentdojo.sh` …
  `table5-overhead.sh`, `all.sh`, `environment.sh` (all executable,
  `bash -n` clean, smoke-run today — table2/3/4 output matches the
  paper cell-for-cell, table5 within the documented ~5% drift), plus
  `requirements.lock` (`pip freeze`, 118 lines: Python 3.11.15,
  agentdojo 0.1.35, pytest 9.1.1, ruff 0.16.8, mypy 2.3.1).
- **Enforcement** (`tests/test_paper.py`, 10 tests): a markdown table
  parser re-derives T2 (stateful `run_demo`), T3 (comparison
  `run_comparison`, both matrices) and T4 (model-loop replay fixtures,
  `importorskip`) in-process and compares cell-by-cell; T1 pinned
  against the AgentDojo page's figures; T5 must equal
  `docs/benchmarks/overhead.md` rung-for-rung; scripts must exist, be
  executable, parse, and reference real (`find_spec`-resolvable)
  modules; lock file must be committed. A benchmark change that moves
  a published number now fails CI until the draft follows.
- **Also**: README "Research & benchmarks" section refreshed (paper
  link first, model-loop + overhead entries added, "what's next" now
  R62); design.md R61 section; CHANGELOG; ROADMAP move.
- **Verification**: pytest full suite green (exit 0; 1,571 collected =
  1,569 passed + 2 env-dependent skips, was 1,559 + 2), ruff clean,
  mypy clean (28 files), `mkdocs build --strict` clean, all seven
  repro scripts smoke-tested.
- **R59 caveat unchanged**: still no `ANTHROPIC_API_KEY` in this
  runner; the paper's T4 provenance warning states it, and the record
  command is committed for a keyed run.

## 明日接續 (next run)

R62 — audit-trace export for TraceSig (first unchecked item in "Up
next"): versioned JSONL trace schema for APG audit logs,
`apg audit export --format tracesig` with documented field mapping,
2–3 example traces under `examples/traces/` (benign session, denied
injection, laundering chain), exporter + round-trip test, schema doc,
examples regenerated by a committed command.
