# Work log — 2026-06-24

## Roadmap item
**R41. `apg audit stats --rule <glob>` rule-id filter.**

The "Up next" queue was exhausted again (R38–R40 done). Following the
project's established, ROADMAP-documented precedent (the R22–R25, R26–R29,
R30–R33, R34–R37 and R38–R40 batches were each proposed when "Up next"
emptied), this run proposes a fresh **R41–R44** batch that continues the
`apg audit stats` filter family, then implements the first item, R41.

## Plan (smallest change satisfying the acceptance criteria)
- Add a pure, exported `filter_by_rule(records, patterns)` helper in
  `audit.py`, an exact mirror of `filter_by_agent` (R36) but matching
  `decision.rule_id` with the `_NO_RULE` sentinel for the default/no-rule
  bucket. fnmatch glob, case-sensitive union, falsy patterns = no filter,
  order-preserving, materialized to a list.
- Export `filter_by_rule` from the package `__init__` (import + `__all__`).
- Wire a repeatable `--rule GLOB` option into `apg audit stats`, applied
  before either renderer (after `--agent`), so every figure reflects the
  subset; composes with `--verdict/--since/--until/--tool/--agent`.
- Add `TestFilterByRule` + `TestAuditStatsRuleCli` to `tests/test_audit_stats.py`
  mirroring the R36 agent tests.

## Files to add/edit
- `src/agent_policy_gateway/audit.py` (new helper)
- `src/agent_policy_gateway/__init__.py` (export)
- `src/agent_policy_gateway/cli.py` (import + wiring + argparse option)
- `tests/test_audit_stats.py` (new tests)
- `ROADMAP.md` (propose R41–R44; move R41 to Done)
- `CHANGELOG.md` (Unreleased → Added)

## Shipped
- New pure helper `filter_by_rule(records, patterns)` in `audit.py`, exact
  mirror of `filter_by_agent` over `decision.rule_id` with the `_NO_RULE`
  sentinel for the default/no-rule bucket.
- Exported `filter_by_rule` from the package `__init__`.
- Wired repeatable `--rule GLOB` into `apg audit stats` (applied after
  `--agent`, before either renderer); composes with all existing filters and
  the `--fail-over` gate.
- 19 new tests (`TestFilterByRule`, `TestAuditStatsRuleCli`).
- Proposed the R41–R44 batch in ROADMAP (Up next exhausted by R40); R41 moved
  to Done.

## Completion summary
Shipped **R41 — `apg audit stats --rule <glob>` rule-id filter**, the final
filter axis completing the verdict/time/tool/agent/rule family. Files touched:
`src/agent_policy_gateway/audit.py`, `src/agent_policy_gateway/__init__.py`,
`src/agent_policy_gateway/cli.py`, `tests/test_audit_stats.py`, `ROADMAP.md`,
`CHANGELOG.md`, and this work-log. Test count 742 -> 761 (+19); pytest 758
passed / 3 skipped, ruff + mypy clean. Commit `__HASH__` —
GitHub: __COMMIT_URL__.
