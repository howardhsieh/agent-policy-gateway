# Work log — 2026-05-21

## Run outcome: ROADMAP fully completed — propose new milestones

Today's scheduled run found **no unchecked items under `## Up next`** in
`ROADMAP.md`. The originally-planned roadmap (R1–R15) is complete on
`origin/main`, closed out by R15 (v0.1.0 release prep) on 2026-05-17.
Per the daily task's Step 3, when no roadmap items remain the run records
the state and proposes new milestones **without pushing any code**. No
commit was made and nothing was pushed.

## Baseline verification (read-only)

A fresh clone of `origin/main` was checked before deciding there was no
work to do, so this entry reflects the canonical repo state rather than
the local mirror:

- **HEAD**: `f8a2a2b` — *Merge pull request #1 from howardhsieh/add-semgrep*.
- **Tests**: `408 passed, 2 skipped` (the 2 skips are environment-gated
  build/packaging checks that require `build` / `twine` to be importable).
- **Lint**: `ruff check .` — clean.
- **ROADMAP**: `## Up next` is empty (with a closing note); `## Done`
  lists R1 – R15.

The project is feature-complete for v0.1.0. The single remaining
*operator* action (not something the scheduled task performs) is the
one-time PyPI trusted-publisher form plus `git tag -a v0.1.0 -m "v0.1.0"
&& git push origin v0.1.0` to actually cut the release — see
`docs/release.md`.

## Proposed new milestones (R16+)

These are proposals for the operator to review and, if approved, move
into `ROADMAP.md` under `## Up next` so future daily runs have work to
pick up. They are ordered roughly by value-to-effort and by how cleanly
each fits the existing reference-monitor design. None of them has been
implemented or committed.

- **R16. Rate-limit enforcement runtime.** R4/R5 mapped the policy
  `rate_limit` action to ALLOW with a note that the counter would land
  later. Implement a sliding-window (or token-bucket) counter keyed by
  `(agent_id, tool_name)` so a rule with `limit_per_minute` actually
  denies the N+1th call inside the window. Acceptance: a policy with
  `rate_limit: { limit_per_minute: 3 }` allows 3 calls and denies the
  4th within 60s, audited as a refusal; counter is monotonic-clock based
  and unit-tested with an injectable clock.

- **R17. `redact` / declassify action.** Today an offending call is
  either allowed or denied. Add an action that *transforms* arguments
  (e.g. masks PII matched by the taint condition) and lets the call
  proceed with a declassified label, recording the transformation in the
  audit record. Acceptance: a `strict-pii`-style rule redacts a matched
  field, the downstream tool sees the masked value, and the audit record
  captures both the rule id and that redaction occurred.

- **R18. `apg policy` CLI: `validate` and `explain`.** A standalone
  console script that (a) validates a policy YAML against the Pydantic
  schema with friendly error messages and exit codes, and (b) `explain`
  takes a tool name + identity + taint label and prints which rule would
  match and why (first-match trace). Acceptance: `apg policy validate
  policies/default.yaml` exits 0; a malformed policy exits non-zero with
  a line-located message; `explain` prints the matched rule id for a
  known web→email scenario.

- **R19. Taint provenance chains.** Extend `TaintLabel` (or carry a
  side-channel) so each label remembers which tool/source introduced it,
  enabling audit records and `apg-replay` to answer "where did the `web`
  taint on this denied send originate?". Acceptance: a 3-hop
  web→summarize→email flow produces an audit record whose provenance
  lists the originating `web_fetch` call id.

- **R20. Policy hot-reload / file-watch.** Allow a long-lived `Gateway`
  to reload its policy when the YAML file changes on disk (opt-in,
  fail-closed if the new file is invalid — keep serving the old policy
  and log the parse error). Acceptance: editing the policy file flips a
  rule's effect for subsequent calls without restarting the process; an
  invalid edit is rejected and the previous policy stays active.

- **R21. LangChain / LlamaIndex tool adapter.** Mirror the existing
  OpenAI / Anthropic / MCP adapters for one more popular agent framework,
  duck-typed so the framework is not a runtime dependency. Acceptance:
  framework tool objects are gateway-wrapped, the canonical
  web→email exfiltration scenario is denied at send, and the adapter has
  structural + scenario tests like the others.

Recommended first pick if the operator approves the batch: **R16**
(rate-limit enforcement) — it closes a known, explicitly-deferred gap in
the existing policy engine and is self-contained.

## Completion summary

**Shipped:** nothing — no roadmap work remained, so per Step 3 the run
recorded the completed state and proposed milestones R16–R21 above for
operator review, without pushing. **Files touched:** this work-log only
(`docs/work-log/2026-05-21.md`), written to the local mirror; not
committed and not pushed. **Baseline on `origin/main`:** 408 passed, 2
skipped; ruff clean; HEAD `f8a2a2b`. **Next up:** awaiting operator
approval to add one or more of R16–R21 to `ROADMAP.md` "Up next"; once an
item is listed, the next daily run will implement it.

---

## Follow-up (operator-approved, same day)

The operator approved the proposed batch and asked for all six to be
added to the backlog (to be worked through over the next few daily
runs). Action taken in this follow-up:

- Replaced the empty-placeholder paragraph under `## Up next` in
  `ROADMAP.md` with six unchecked items **R16–R21**, each carrying its
  description and explicit acceptance criteria, in the
  `- [ ] **R<n>. ...**` format the daily task's Step 3 selects on.
- Recommended order stands: **R16** (rate-limit enforcement) is the
  first unchecked item and will be picked up by the next daily run.
- This is a docs/roadmap-only change; no source or tests were modified.
  `pytest` (408 passed, 2 skipped) and `ruff check .` remain green.
- Committed and pushed to `origin/main`; mirrored back to the local
  Github folder.
