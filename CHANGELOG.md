# Changelog

All notable changes to this project will be documented in this file. The format is based
on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/).

## [Unreleased]

### Added
- **Release runbook (R14a)**: new `docs/release.md` documents the end-to-end release procedure for publishing `agent-policy-gateway` to PyPI. R14 was split into R14a (docs-only, this commit) and R14b (the actual `.github/workflows/publish.yml` file), so the procedure is reviewable and the manual `python -m build` + `python -m twine check` + `python -m twine upload` fallback path is a complete, working release path today — independent of any GitHub Actions workflow. The runbook has three parts: one-time PyPI trusted-publisher configuration (the on-PyPI pending-publisher form with the `pypi` GitHub environment name committed up front, so R14b's workflow file and the PyPI side already agree), the steady-state automated flow that R14b will enable (bump `pyproject.toml` version, move the `[Unreleased]` block in this file under a new `[<version>] — YYYY-MM-DD` heading, tag the commit with `v<version>`, push, watch the `test → build → publish` workflow), and the manual fallback. A post-release verification checklist closes the page: the project page on PyPI shows both an sdist and a wheel, `pip install agent-policy-gateway==<version>` succeeds in a clean virtualenv, the package imports, and the `apg-replay --help` / `apg-bench --help` console scripts exit 0. Wired into mkdocs `nav` between Daily runbook and the implicit work-log group; linked from a new *Release* section in `README.md` (which is honest about R14b still being pending) and a new *Cut a release* row in `docs/index.md`. `docs/design.md` gains a 'Release runbook (R14a)' note recording the split rationale and three design decisions: trusted publishing is the destination (no `PYPI_API_TOKEN` ever lives in the repo or in GitHub secrets), the manual fallback is intentionally written as a complete release path so the project can ship today, and test scope mirrors commit scope (workflow-file invariants land with R14b in `tests/test_publish_workflow.py`, not now). 20 new tests in `tests/test_release_runbook.py` lock in the runbook invariants without constraining prose: file presence, non-trivial size, ATX H1, the five required sections (`## 1. One-time setup`, `## 2. Cutting a release`, `## 3. Manual fallback`, `## Verification`, `## What lives where`) appear in declared order, the runbook describes trusted publishing, the manual-fallback commands (`python -m build`, `python -m twine upload`) are spelled out verbatim, `twine check` runs before upload, the `pypi` environment name is committed, the runbook acknowledges the R14b split so a reader knows the workflow file is forthcoming, the verification checks (clean-virtualenv `pip install`, `apg-replay --help`, `apg-bench --help`) are documented, the page is in mkdocs `nav`, the README has a ## Release section linking to `docs/release.md`, `docs/index.md` adds the *Cut a release* row, `docs/design.md` records the 'Release runbook (R14a)' note, and every relative markdown link from the runbook resolves to a real file on disk. Test count delta: 369 -> 389 (+20); ruff clean. Diff is intentionally docs-only — no source file is touched.
- **Project threat model (R13)**: new `docs/threat-model.md` documents the project-level threat model — assets the gateway protects (sensitive resources reachable through tools, confidential data flowing across tools, audit-trail integrity, operator policy), the trust boundary the reference monitor enforces (LLM/agent process untrusted; gateway trusted; tools trusted to do what their description says but their *outputs* are taint-labelled; operator trusted with policy authorship), and eight enumerated adversary classes (A1 indirect prompt injection from tool output — the project's primary concern; A2 direct prompt injection from the user; A3 compromised or malicious tool implementation; A4 compromised model weights / model jailbreak; A5 cross-agent / cross-session bleed; A6 malicious operator with policy-edit rights — explicitly out of scope; A7 host / sandbox escape — explicitly out of scope; A8 audit-log tampering by the agent process). Documents the deployer's assumptions (every tool call goes through the gateway; operator-declared taint specs are honest; the audit writer is durable enough; the policy file is under version control), the in-scope and out-of-scope attacker capabilities, the canonical abuse scenarios mapped to mitigations the gateway already ships (web-to-email exfiltration, cross-tool taint laundering, PII spillage to a public channel, identity spoofing in publish gating, malformed/oversized arguments, audit-log gap on a denied call, stale or missing taint declaration on a new tool), and the residual risks (policy-author error, coarse taint granularity, channel confusion via aliased resources, long-lived agent context). Wired into the mkdocs `nav` between Design and Benchmarks; linked from `README.md` (expanded Threat model section) and `docs/index.md` (new row in the where-to-start table). `docs/design.md` gains a short "Threat model (R13)" note recording the design decision: the threat model is a project-level promise, not a per-feature appendix, so it lives in its own file and is referenced from feature docs rather than restated inline. 26 new tests in `tests/test_threat_model.py` lock in the structural invariants without constraining prose: the file exists and is non-trivial in size, starts with an ATX H1, the eight required top-level sections (`## Assets`, `## Trust boundaries`, `## Adversary classes`, `## Assumptions`, `## In-scope attacker capabilities`, `## Abuse scenarios and mitigations`, `## Residual risks`, `## Out of scope`) are present and appear in the documented order, all eight adversary IDs (`A1` through `A8`) are enumerated, the page is referenced from the mkdocs `nav` (validated by walking the parsed yaml), the page is referenced from `README.md` and `docs/index.md`, every relative markdown link target on the page resolves to a file that actually lives in the repo, and `docs/design.md` records the R13 design note. Test count delta: 343 -> 369 (+26); ruff clean.
- **Benchmark harness (R12)**: new `agent_policy_gateway.bench` module — a standard-library-only timing harness exposing a frozen `BenchResult` dataclass (name, iterations, warmup, total/mean/min/max/p50/p95/p99 in nanoseconds, derived ops/sec, plus `to_dict`) and a `benchmark(name, fn, *, iterations, warmup=0)` runner that brackets every call with `time.perf_counter_ns()` so percentiles reflect real per-call latency rather than wall-clock-divided averages. Four canonical scenarios span the gateway's hot paths: `raw_call` (baseline tool body, no gateway), `gateway_allow` (`Gateway.execute` + `wrap_tool` against an allow policy, no audit), `gateway_deny` (deny policy; the timed callable swallows `PolicyDenied` so percentiles measure refusal cost, not exception unwinding), and `gateway_allow_audit` (an in-process no-op `AuditWriter` to isolate audit-call dispatch from `JsonlAuditWriter`'s JSON+fsync cost). Public helpers `run_default_suite(*, iterations, warmup, scenarios=None)`, `format_results_table(results)` (fixed-width µs table), `results_to_json(results)` (stable JSON document), and `make_scenario(name)` round out the module. New `apg-bench` console script wired in `[project.scripts]` -> `agent_policy_gateway.bench:bench_main` with flags `--iterations N`, `--warmup N`, `--scenario NAME` (repeatable, validated against the scenario set), and `--json`. Tool body (`x + y`) is held constant across scenarios so the gateway-allow vs raw-call delta is mostly attributable to the gateway. New `docs/benchmarks.md` documents the scenarios, methodology, and programmatic API; the page is wired into `mkdocs.yml` `nav`. README gains a "Benchmarks" section. `docs/design.md` gains a "Benchmark harness (R12)" note explaining the per-call timing decision, the constant-tool-body invariant, why the default audit scenario uses an in-process writer, and why the tests assert structure rather than absolute thresholds. New public exports: `BenchResult`, `benchmark`, `run_default_suite`, `format_results_table`, `results_to_json`, `bench_main`. 30 new tests in `tests/test_bench.py` lock in the invariants without flaky timing assertions: `min <= p50 <= p95 <= p99 <= max`; iterations + warmup count is exact; `total_ns / iterations == mean_ns`; ops/sec is positive; `_percentile_ns` rejects empty / out-of-range / known-rank inputs as expected; `raw_call`/`gateway_allow`/`gateway_allow_audit` return the underlying `1+2 = 3`; `gateway_deny` swallows `PolicyDenied` so the harness loop never raises (and verifies the underlying gateway *does* raise when called directly); the audit scenario invokes its writer once per call; `make_scenario` rejects unknown names; `run_default_suite` returns one `BenchResult` per scenario in input order; `format_results_table` includes headers, scenario names, and a separator; `results_to_json` round-trips through `json.loads`; the CLI default invocation prints the table; `--json` produces parseable JSON with one entry per scenario; `--scenario raw_call --json` filters to one result; unknown scenario name, zero `--iterations`, and negative `--warmup` all exit 2.
- **Documentation site (R11)**: new `mkdocs.yml` at the repo root configures an [mkdocs-material](https://squidfunk.github.io/mkdocs-material/) site for the project, with the `material` theme, light/dark palette toggle, navigation tabs, and the standard `pymdownx` extension stack (admonitions, superfences, code-copy, tabbed content, snippets). New `docs/index.md` (homepage) and `docs/quickstart.md` (a copy-pasteable end-to-end walkthrough that wires `web_fetch` and `send_email` behind a Gateway loaded from a tiny YAML policy and watches `deny-web-to-email` fire) round out the curated nav, alongside the existing `docs/design.md` and `docs/daily-runbook.md`. The `not_in_nav` directive keeps `docs/work-log/*` out of the navigation without producing nav-coupling warnings under `mkdocs build --strict`. New `[project.optional-dependencies] docs` extra pins `mkdocs>=1.5`, `mkdocs-material>=9.5`, and `pymdown-extensions>=10` so contributors can `pip install -e ".[docs]"` and run `mkdocs serve` for a live-reloading preview. README gains a Documentation section linking the site sources and the build command; `docs/design.md` gains a short "Docs site (R11)" note. 11 new tests in `tests/test_docs_site.py` lock in the invariants: `mkdocs.yml` parses as YAML, has the required top-level keys (`site_name`, `theme`, `nav`), uses the `material` theme, points its `repo_url` at github.com, the configured `docs_dir` exists, `docs/index.md` and `docs/quickstart.md` are present, every `nav` file reference resolves to an actual file under `docs/` (including the homepage), and — when `mkdocs` and `mkdocs-material` are importable — `python -m mkdocs build --strict` against `mkdocs.yml` exits 0 and produces `index.html` and `quickstart/index.html` (the build test skips otherwise so the core matrix stays dependency-light).
- **Worked indirect-injection example (R10)**: new `examples/indirect_injection/` package that demonstrates the canonical defence end-to-end. A poisoned web page (`injected_page.py`) carries a hidden instruction telling the agent to read an internal `launch-codes` record and email its contents to `attacker@evil.example`. Three in-memory tools (`web_fetch`, `kb_lookup`, `send_email` writing to an in-memory `Outbox`) plus a deterministic stub agent (`agent.py`) make the failure mode reproducible without an LLM. Two runners — `naive.py` (raw functions; the email is sent and the secret leaks) and `gated.py` (every tool wrapped by a `Gateway` configured with the shipped `policies/default.yaml`; `web_fetch` registered with `ToolTaintSpec.of(adds=('web',))`; the `deny-web-to-email` rule refuses `send_email` because its inputs carry the `web` source) — and a single-command entry point (`python -m examples.indirect_injection`) print a one-line outcome for each variant and exit 0 when both behave as expected. 10 new tests in `tests/test_example_indirect_injection.py` lock in the acceptance criteria: naive runner exfiltrates `TOP-SECRET-LAUNCH-CODES-42` to the attacker; gated runner refuses with rule id `deny-web-to-email` and the outbox stays empty; audit log captures the DENY decision and the upstream allows; `agent_id` and `call_id` are recorded; the page invariants hold; the `__main__` subprocess exits 0 with the expected stdout. Public, importable surface (`run_naive`, `run_gated`, `build_gateway`, `INJECTED_PAGE_URL`, `INJECTED_PAGE_HTML`, `ATTACKER_EMAIL`) makes the example double as an integration sample.
- **Async gateway path (R9)**: new `Gateway.aexecute(call, fn, *args, …)` and `Gateway.wrap_tool_async(…)` for `async def` tools. `aexecute` awaits the underlying callable's return when it is awaitable and awaits the audit writer's return when it is awaitable, so async log sinks plug in without a thread shim while synchronous writers keep working unchanged; the fail-closed-on-audit / DENY-or-REVIEW-raises contract matches `execute` exactly. `wrap_tool_async` mirrors `wrap_tool` (reserved kwargs, `resource_arg` binding, taint-spec registration) but returns an `async def` wrapper that routes through `aexecute`. New `agent_policy_gateway.mcp_async_adapter` module exposes `wrap_mcp_session_async(gateway, session, *, taint_specs=None, resource_args=None, prefix=None)` for duck-typed async MCP-style sessions exposing `await session.list_tools()` and `await session.call_tool(name, arguments)` (the real `mcp` SDK shape — the SDK remains an *optional* dependency). Sync descriptor helpers `_iter_tool_descriptors` / `_tool_name` are reused so prefixing, dict/attribute-shape support, and duplicate-name detection match `wrap_mcp_session`. Updated `mcp_adapter.py` docstring to point at the new async helper. 40 new tests across `tests/test_gateway_async.py` and `tests/test_mcp_adapter_async.py` cover allow / deny / review through `aexecute`, sync and async audit writers, audit-failure abort, kwarg stripping, resource binding (and `apg_resource` override), taint propagation, prefixing, dict and attribute descriptors, every misshapen-session error edge, and a worked async web→summarize→email exfiltration scenario that the gateway denies at send. Public exports: `Gateway.aexecute`, `Gateway.wrap_tool_async`, `wrap_mcp_session_async`.
- **Anthropic tool-use adapter (R8)**: new `agent_policy_gateway.anthropic_adapter` module exposing `AnthropicTool` (frozen tool description: name, description, JSON Schema `input_schema`, Python callable, optional `resource_arg` / `taint_spec`), `anthropic_tool_specs(tools, *, prefix=None) -> list[dict]` (builds the flat Messages-API `tools=[...]` array, `{name, description, input_schema}` — no `type`/`function` wrapper), `wrap_anthropic_tools(gateway, tools, *, prefix=None) -> dict[str, Callable]` (gateway-wraps each tool and registers any per-tool `ToolTaintSpec` under the prefixed name), and `dispatch_anthropic_tool_use` / `dispatch_anthropic_tool_uses` for converting model-produced `tool_use` content blocks into `tool_result` content blocks. Both dict-form payloads (raw HTTP responses) and attribute-form objects (the anthropic-python SDK's `ToolUseBlock` pydantic models) are accepted; the real `anthropic` package is **not** a dependency. ``input`` arrives as a parsed dict but a JSON-encoded string is also tolerated. Reserved kwargs (`apg_input_label`, `apg_agent_id`, `apg_call_id`, `apg_resource`) stay out of advertised JSON schemas; `apg_call_id` is auto-populated from the tool_use `id` so audit lines up with the model's view. `PolicyDenied` / `PolicyReview` become `tool_result` blocks with `is_error: true` and a structured `{"error":"policy_refusal","verdict":...,"rule_id":...,"reason":...}` payload (customisable via `on_denied`); tool exceptions and malformed input (`AnthropicToolUseError`) intentionally raise. 36 new tests in `tests/test_anthropic_adapter.py` cover construction validation, spec shape and prefixing, allow / deny / review dispatch (dict and attribute forms), JSON-string input tolerance, missing/wrong block-type errors, taint propagation through the adapter, resource-arg binding (and `apg_resource` override), batched dispatch, and a worked web→email exfiltration scenario. Public exports: `AnthropicTool`, `AnthropicToolUseError`, `anthropic_tool_specs`, `wrap_anthropic_tools`, `dispatch_anthropic_tool_use`, `dispatch_anthropic_tool_uses`.
- **OpenAI function-calling adapter (R7)**: new `agent_policy_gateway.openai_adapter` module exposing `OpenAITool` (frozen tool description: name, description, JSON Schema parameters, Python callable, optional `resource_arg` / `taint_spec`), `openai_tool_specs(tools, *, prefix=None) -> list[dict]` (builds the JSON `tools=[...]` array Chat Completions / Responses expect), `wrap_openai_tools(gateway, tools, *, prefix=None) -> dict[str, Callable]` (gateway-wraps each tool and registers any per-tool `ToolTaintSpec` under the prefixed name), and `dispatch_openai_tool_call` / `dispatch_openai_tool_calls` for converting model-produced `tool_calls` into `role="tool"` messages. Both dict-form payloads (raw HTTP responses) and attribute-form objects (the openai-python SDK's pydantic models) are accepted; the real `openai` package is **not** a dependency. Reserved kwargs (`apg_input_label`, `apg_agent_id`, `apg_call_id`, `apg_resource`) stay out of advertised JSON schemas; `apg_call_id` is auto-populated from the OpenAI `tool_call.id`. `PolicyDenied` / `PolicyReview` become structured tool-role error payloads (`{"error":"policy_refusal","verdict":...,"rule_id":...,"reason":...}`) so the model can react to refusals; tool exceptions and malformed input (`OpenAIToolCallError`) intentionally raise. 34 new tests in `tests/test_openai_adapter.py` cover construction validation, spec generation and prefixing, allow / deny / review dispatch, JSON parse errors, unknown tool, taint propagation through the adapter, resource-arg binding (and `apg_resource` override), batched dispatch, and a worked web→email exfiltration scenario. Public exports: `OpenAITool`, `OpenAIToolCallError`, `openai_tool_specs`, `wrap_openai_tools`, `dispatch_openai_tool_call`, `dispatch_openai_tool_calls`.
- **MCP adapter (R6)**: new `agent_policy_gateway.mcp_adapter` module exposing `wrap_mcp_session(gateway, session, *, taint_specs=None, resource_args=None, prefix=None)`. One call discovers every tool advertised by an MCP-compatible session (duck-typed against sync `list_tools()` / `call_tool(name, arguments)`; the real async `mcp` SDK is *not* a dependency, that path is reserved for R9), registers any per-tool `ToolTaintSpec`, and returns a `dict[str, Callable]` of gateway-mediated callables. Supports both bare-iterable and `.tools`-bearing `list_tools` shapes, attribute- and dict-form descriptors, an optional `prefix` for namespacing multiple sessions on one gateway (the advertised name is still used for the underlying call), per-tool `resource_args` for selector matching with the `apg_resource` override taking precedence, and the same four reserved kwargs as the core gateway (`apg_input_label`, `apg_agent_id`, `apg_call_id`, `apg_resource`) — stripped before forwarding. Misshapen sessions (missing methods, malformed descriptors, duplicate names) raise clear `TypeError` / `ValueError` upfront. 21 new tests in `tests/test_mcp_adapter.py` cover discovery, prefixing, allow + deny paths, taint propagation through registered specs, resource-arg binding (and override), audit-writer integration, and every error edge. Public export: `wrap_mcp_session`.
- **Append-only JSONL audit log + replay tool (R5)**: new `agent_policy_gateway.audit` module with `JsonlAuditWriter` (a callable compatible with the gateway's `AuditWriter` slot, opening the target file in append mode, flushing after every record, optional `fsync=True` for durability), `AuditRecord` (frozen `ts` / `call` / `decision` round-tripping through `to_dict` / `from_dict`), and `read_audit` (eager open, blank-line tolerant, raises `AuditFormatError` with line numbers on malformed JSON or missing keys). New `apg-replay` console script (entry point in `pyproject.toml`) reads a log and prints a human-readable timeline; supports `--verdict {allow,deny,review}` and `--limit N` filters and returns 0 / 2 / 3 for ok / missing-file / malformed-line. 26 new tests in `tests/test_audit.py` cover writer append semantics across reopens, parent-directory creation, single-line invariant under multi-line args, gateway integration on allow + deny paths, end-to-end exfiltration round-trip through the replay CLI, and the CLI's filter and exit-code behaviour. Public exports: `JsonlAuditWriter`, `AuditRecord`, `AuditFormatError`, `read_audit`, `replay_main`, `format_record`.
- **Reference monitor (R4)**: new `agent_policy_gateway.gateway` module with the `Gateway` class and `@gw.wrap_tool` decorator. The gateway evaluates policies in order (first match within a policy and across policies), propagates taint via `taint.propagate` to compute the output label on every decision, and writes an audit record before the wrapped tool runs (fail-closed-on-audit). Refusals raise `PolicyDenied` or `PolicyReview`, both carrying the originating `ToolCall` and `Decision`. The decorator recognises four reserved kwargs (`apg_input_label`, `apg_agent_id`, `apg_call_id`, `apg_resource`) and strips them before forwarding; `resource_arg` ties policy resource matching to a named parameter of the wrapped function via `inspect.signature`. `Action.RATE_LIMIT` maps to `Verdict.ALLOW` until the counter lands with R5. 27 new tests in `tests/test_gateway.py`, including a worked web→summarize→email exfiltration that the gateway denies at send. New public exports: `Gateway`, `GatewayError`, `PolicyDenied`, `PolicyReview`, `AuditWriter`, and the four reserved-kwarg constants.
- **Policy DSL v0 (R3)**: declarative YAML policies via the new
  `agent_policy_gateway.policy` module. Frozen Pydantic models —
  `Policy`, `Rule`, `Selector`, `Effect`, `TaintCondition`, `Action` —
  define a versioned schema with selectors over tool name (fnmatch
  glob), identity, resource glob, and three-clause taint conditions
  (`all_of` / `any_of` / `none_of`), and effects covering
  `allow` / `deny` / `review` / `rate_limit`. `load_policy`,
  `load_policy_str`, and `load_policies` parse and validate YAML,
  surfacing structural problems as `PolicyError`. Three example
  policies ship under `policies/`: `default.yaml` (baseline
  exfiltration prevention), `research-agent.yaml` (identity-gated
  publish, rate-limited search, reviewed external POST), and
  `strict-pii.yaml` (PII redaction-aware deny rules). 44 new tests in
  `tests/test_policy.py` cover schema validation, loader error paths,
  and first-match behaviour against the shipped examples.
- **Taint propagation algebra** (R2): `agent_policy_gateway.taint` module with
  `join` / `join_all`, `subsumes`, `flows_to`, `ToolTaintSpec`, and `propagate`.
  The propagation rule is `output = ((∨ inputs) ∨ spec.adds) \ spec.declassifies`,
  giving the gateway a pure function for computing per-call output taint. New
  helpers are re-exported from the package root. 31 new tests in
  `tests/test_taint.py` cover lattice algebra (identity, commutativity,
  associativity, idempotence), order properties of `subsumes`, propagation
  rules including declassification, and a worked end-to-end exfiltration
  example (`web_search → summarize → send_email` is denied).
- **Core data model** (R1): `TaintLabel`, `ToolCall`, `Decision`, `Verdict` in
  `agent_policy_gateway.core`, with `to_json` / `from_json` round-tripping and
  34 unit tests covering construction, value equality, lattice algebra
  (join idempotent / commutative / associative, subsumption), and JSON stability.
- Initial project scaffold: README, ROADMAP, LICENSE, design notes, daily runbook,
  smoke test, `pyproject.toml`.

[Unreleased]: about:blank
