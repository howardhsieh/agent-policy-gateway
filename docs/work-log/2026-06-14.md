# Work log — 2026-06-14

## Roadmap item
**R35. `apg audit stats --tool <glob>` tool-name filter.**

## Plan (smallest change satisfying acceptance criteria)
- Add a pure `filter_by_tool(records, patterns)` helper to `audit.py`, beside
  `filter_by_verdict` / `filter_by_time`: falsy `patterns` → no filter; otherwise
  keep records whose `call.tool_name` matches any pattern via
  `fnmatch.fnmatchcase` (union); preserve input order; materialize to a list.
- Export `filter_by_tool` from `audit.py` `__all__` and from the package
  `__init__.py`.
- Wire a repeatable `--tool PATTERN` option onto the `apg audit stats` parser
  and apply `filter_by_tool` in `_cmd_audit_stats` *before* the renderers, so it
  composes with `--verdict` / `--since` / `--until`.
- Add unit tests for `filter_by_tool` (no filter, single glob, literal,
  multi-pattern union, no-match → empty, order preserved) and CLI tests
  (text + `--json`, composes with `--verdict`, empty result) in
  `tests/test_audit_stats.py`.

## Files to add/edit
- `src/agent_policy_gateway/audit.py` (helper + export + `import fnmatch`)
- `src/agent_policy_gateway/__init__.py` (export)
- `src/agent_policy_gateway/cli.py` (option + apply + import)
- `tests/test_audit_stats.py` (unit + CLI tests)
- `ROADMAP.md`, `CHANGELOG.md` (bookkeeping)

## Shipped
- `filter_by_tool(records, patterns)` added to `audit.py` (pure, fnmatch union,
  case-sensitive, falsy → no filter, order preserved) and exported from both
  `audit.py` `__all__` and the package `__init__.py`.
- `apg audit stats --tool GLOB` option (repeatable, argparse `action="append"`)
  wired into `_cmd_audit_stats`, applied before the renderers so it composes
  with `--verdict` / `--since` / `--until`.
- 14 new tests (`TestFilterByTool`, `TestAuditStatsToolCli`).
- Verification: pytest 666 -> 680 passed (+14), 3 skipped; ruff clean; mypy clean.

## Completion summary
Shipped roadmap item **R35** (`apg audit stats --tool <glob>` tool-name filter). Files touched: `src/agent_policy_gateway/audit.py` (new `filter_by_tool` helper + export + `import fnmatch`), `src/agent_policy_gateway/__init__.py` (export), `src/agent_policy_gateway/cli.py` (repeatable `--tool` option + apply before renderers), `tests/test_audit_stats.py` (+14 tests), plus `ROADMAP.md`, `CHANGELOG.md` bookkeeping. Test count delta 666 -> 680 (+14 tests), 3 skipped; ruff + mypy clean. Commit `c969cdc`. GitHub commit URL: https://github.com/howardhsieh/agent-policy-gateway/commit/c969cdc021f60ed0502c8b179dfcc501520d7f0c
