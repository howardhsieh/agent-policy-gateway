# Work log — 2026-05-09

## Roadmap item
**R13. Threat-model document with adversary classes and assumptions.**

## Plan

- Add `docs/threat-model.md` as the canonical project threat model. It
  documents the assets we are protecting, the trust boundaries the
  gateway enforces, the adversary classes the gateway is designed to
  resist (and which it is *not*), the system assumptions a deployer is
  signing up for, the in-scope attacker capabilities and the abuse
  scenarios mapped to mitigations the gateway already ships, and the
  residual risks and out-of-scope concerns.
- Wire the new page into the mkdocs `nav` between Design and
  Benchmarks so it appears alongside the architecture material rather
  than tucked behind the daily runbook.
- Add a short "Threat model" row to `docs/index.md` so the where-to-start
  table on the docs homepage points at the new page; expand the
  existing two-paragraph "Threat model" section in `README.md` so
  external readers know the deeper write-up exists and where to find
  it.
- Add a short "Threat model (R13)" note to `docs/design.md` recording
  the design decision: the threat model is a project-level promise,
  not a per-feature appendix, so it lives in its own file and is
  referenced from feature docs rather than restated inline.
- Add `tests/test_threat_model.py` that locks in the invariants future
  edits must preserve: the file exists; it is non-trivial in size;
  it uses ATX headings; the canonical top-level sections are all
  present (`## Assets`, `## Trust boundaries`, `## Adversary classes`,
  `## Assumptions`, `## In-scope attacker capabilities`,
  `## Abuse scenarios and mitigations`, `## Residual risks`,
  `## Out of scope`); the page is referenced from the mkdocs `nav`;
  the page is referenced from `README.md` and `docs/index.md`; and
  every relative markdown link target on the page resolves to a file
  that exists in the repo.

## Files I intend to touch

- `docs/threat-model.md` (new).
- `docs/index.md` (add row to the where-to-start table).
- `docs/design.md` (short "Threat model (R13)" note).
- `README.md` (expanded Threat model section linking the new page).
- `mkdocs.yml` (nav entry).
- `tests/test_threat_model.py` (new).
- `ROADMAP.md`, `CHANGELOG.md`, this work-log.

## Shipped

R13 ships the project-level threat model as `docs/threat-model.md`,
documenting the assets the gateway protects, the trust boundary the
reference monitor enforces, eight enumerated adversary classes
(A1 indirect prompt injection through the lower-half being our primary
concern; A6/A7 explicitly out of scope), the deployer assumptions the
safety claims rest on, the in-scope and out-of-scope attacker
capabilities, the canonical abuse scenarios mapped back to mitigations
the gateway already ships, and the residual risks. The page is wired
into the mkdocs `nav` between Design and Benchmarks; linked from
`README.md` and the docs homepage; `docs/design.md` gains a short
"Threat model (R13)" note recording the design decision (project-level
promise, not per-feature appendix). 26 new tests in
`tests/test_threat_model.py` lock in structural invariants without
constraining prose. Test count delta: 343 → 369 (+26); ruff clean.

## Final report

**Roadmap item shipped:** R13. Threat-model document with adversary
classes and assumptions.

**Files touched:** `docs/threat-model.md` (new, 287 lines);
`tests/test_threat_model.py` (new, 26 tests, 248 lines);
`mkdocs.yml` (`Threat model` nav entry between Design and Benchmarks);
`README.md` (expanded Threat model section linking the new page);
`docs/index.md` (new row in the where-to-start table);
`docs/design.md` (`Threat model (R13)` design note);
`CHANGELOG.md` and `ROADMAP.md` updated;
`docs/work-log/2026-05-09.md` (this file).

**Test count delta:** 343 → 369 (+26); 1 skipped (mkdocs build test
requires the `docs` extra which is not installed in the daily CI matrix).

**Ruff:** clean.

**Commit hash:** 342d0d1 (full: 342d0d1f4690fba4be2f8d19c03aabed4569526e).

**GitHub commit URL:** https://github.com/howardhsieh/agent-policy-gateway/commit/342d0d1f4690fba4be2f8d19c03aabed4569526e
