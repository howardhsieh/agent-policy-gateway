# 2026-05-07 — R11: Documentation site (mkdocs-material)

## Plan

- Add `mkdocs.yml` at the repo root, configured with the `material` theme,
  `site_name`, repo URL, and a curated `nav` referencing the existing
  `docs/` content (design, daily-runbook, work-log).
- Add `docs/index.md` as the documentation homepage — short pitch + links
  to README/ROADMAP/quickstart, no duplication of full README content.
- Add `docs/quickstart.md` distilling the README's quick-start snippet
  into a runnable, gateway-flavoured walkthrough so a first-time reader
  can copy-paste and see the gateway refuse a flow.
- Add an `[project.optional-dependencies] docs` group (`mkdocs`,
  `mkdocs-material`) so contributors can `pip install -e .[docs]`.
- Add `tests/test_docs_site.py` validating the site structure: mkdocs.yml
  parses as YAML, has `site_name`, uses the `material` theme, every `nav`
  file reference resolves to an actual file under `docs/`, and `mkdocs
  build` is exercised in a tmp dir if `mkdocs` is importable (skipped
  otherwise so the core test suite stays dependency-light).
- Update `README.md` with a one-line link to the docs site source and
  mention the `docs` extra. Update `docs/design.md` with a short "Docs
  site (R11)" note.

## Files

- new: `mkdocs.yml`
- new: `docs/index.md`
- new: `docs/quickstart.md`
- new: `tests/test_docs_site.py`
- edit: `pyproject.toml` (add `docs` extra)
- edit: `README.md` (link to docs build, mention extras)
- edit: `docs/design.md` (R11 note)
