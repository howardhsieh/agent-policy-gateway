# 2026-09-03 — Plan complete; awaiting the next plan revision (day 2)

**Item.** None picked. `origin/main` gained no new plan revision since
yesterday's report (HEAD = `c5236c4`), so the 2026-08-21 plan revision (as
amended 2026-08-25) remains **complete** — R49–R56 all in ROADMAP "Done",
"Up next" empty, R48 still parked and ineligible. Per the standing hand-off
note, this run verifies the repo is green, reports, and waits rather than
inventing scope.

## Verification (repo health check)

Confirmed the repo is fully green at `origin/main` (c5236c4):

- `python -m pytest` — **1455 passed, 2 skipped** (with `agentdojo`
  installed; unchanged from 2026-09-02).
- `python -m ruff check .` — clean.
- `python -m mypy src/agent_policy_gateway` — clean (23 source files).
- `python -m mkdocs build --strict` — clean.

No code changes made.

## Draft plan revision (proposal — NOT adopted; awaiting Howard)

To make the next revision a paste-and-go edit, yesterday's candidate list is
firmed up into ROADMAP-shaped items with acceptance criteria. These are a
*proposal only*; none is added to "Up next".

- [ ] **R57. Per-value taint labels.** Attach labels to values (tool-call
  outputs and the argument strings derived from them), not just the session
  input, so a sink decision can see the label of the value actually flowing
  into each argument. Acceptance: a value-flow API the runtime threads
  through tool results → subsequent arguments (exact-match propagation is
  enough for the PoC); policy sub-condition to match per-argument value
  labels; re-run the R56 comparison family with a `value-taint` arm and
  publish whether it separates covert-launder compromise from benign-launder
  utility (the measured impossibility) and closes the chain arms'
  exfiltration residual; tests + docs/benchmarks update.

- [ ] **R58. Model-in-the-loop long-horizon eval.** Run a real LLM agent
  (any provider behind a thin driver interface, with a recorded/replay mode
  so CI stays deterministic and key-free) through the R55 stateful harness
  on AgentDojo-composed scenarios via `scenario_from_suite`. Acceptance:
  driver interface + replay fixtures committed; utility/ASR for no-defense
  vs one APG arm on a small task slice; write-up of how policy pressure
  changes agent behavior (refusal handling, retries) in
  `docs/benchmarks/`.

- [ ] **R59. External write-up packaging.** Turn
  `docs/benchmarks/comparison.md` + the R50/R53 AgentDojo numbers into a
  self-contained workshop-style write-up: related-work section (Progent,
  Fides, CaMeL, f-secure/IsolateGPT lineage), one reproducibility script
  per figure/table, pinned environment. Acceptance: `docs/paper/` draft
  builds; every number in it regenerated by a committed command.

## 明日接續 (next run)

Same protocol: if ROADMAP "Up next" has gained items, resume the
one-item-per-day cadence with the first unchecked item; otherwise verify
green and report. If the draft above is adopted verbatim, the next run
starts R57.
