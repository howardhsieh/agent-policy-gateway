# Work log — 2026-06-02

## Roadmap item
**R23. Typed-API regression guard in CI.**

## Plan
- Add a `[tool.mypy]` baseline to `pyproject.toml` (`python_version`, `warn_unused_ignores`), kept permissive so the current tree passes clean.
- Add a `Run mypy` step to the `test` job in `.github/workflows/publish.yml`, invoking `mypy src/agent_policy_gateway`.
- Document the `make`/`tox`-free local equivalent (`mypy src/agent_policy_gateway`) in the README's Contributing section.
- Add structural tests asserting the workflow mypy step exists and that the pyproject baseline is pinned.

## Files touched
- `pyproject.toml` — added `[tool.mypy]` + `yaml` override.
- `.github/workflows/publish.yml` — new `Run mypy` step in `test` job.
- `README.md` — Local checks subsection.
- `tests/test_publish_workflow.py` — two new structural tests.
- `ROADMAP.md`, `CHANGELOG.md` — bookkeeping.

## Completion summary
Shipped **R23. Typed-API regression guard in CI.** Added a permissive `[tool.mypy]` baseline to `pyproject.toml` (`python_version = "3.10"`, `warn_unused_ignores = true`, `files = ["src/agent_policy_gateway"]`, plus a `yaml` `ignore_missing_imports` override) and a `Run mypy` step (`python -m mypy src/agent_policy_gateway`) in the `test` job of `.github/workflows/publish.yml`; documented the `make`/`tox`-free local equivalent in the README's new *Local checks* subsection. Files touched: `pyproject.toml`, `.github/workflows/publish.yml`, `README.md`, `tests/test_publish_workflow.py`, plus `ROADMAP.md` / `CHANGELOG.md` / this work-log. Test count delta: 518 -> 520 (+2 structural tests); ruff clean; mypy clean. Commit `5ac1362` (https://github.com/howardhsieh/agent-policy-gateway/commit/5ac136241903b3f6cc744984313c4403bd1432ce).
